apiVersion: v1
kind: Secret
metadata:
  name: help-centre-api-secrets
  labels:
    {{- include "help-centre-api.labels" . | nindent 4 }}
type: Opaque
data:
  # Note: In production, these should be managed by external secret management
  # systems like AWS Secrets Manager, HashiCorp Vault, or Sealed Secrets
  # Base64 encoded values - replace with actual values
  openai-api-key: ""  # Add: echo -n "your-key" | base64
  pinecone-api-key: ""  # Add: echo -n "your-key" | base64